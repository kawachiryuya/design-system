# Dependencies
node_modules/

# Build outputs
storybook-static/
dist/

# Incremental typecheck cache (tsc --noEmit --incremental)
*.tsbuildinfo

# Style Dictionary build artifacts (auto-generated from tokens/source/)
tokens/build/
# Legacy-format JSON outputs (regenerated by tokens:build for backward compat)
# Source of truth is tokens/source/ ; these direct-children .json are derived.
tokens/*.json
# Exception: contrast-pairs.json is hand-authored source (check:contrast), not generated.
!tokens/contrast-pairs.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# iCloud
*.icloud
.icloud-nosync

# Editor
.vscode/
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Environment
.env
.env.local
.env.*.local
.vercel

# MCP config
.mcp.json

# Claude Code per-user local settings (個人マシン固有の許可リスト等)
.claude/settings.local.json

# npm authentication（PAT を含む可能性、絶対 commit しない）
.npmrc

# ローカル作業メモ（追跡しない。docs/layout-patterns-inventory.md は追跡対象のまま）
docs/CLAUDE.md-addition/
docs/design-system-backlog.md
docs/design-system-contrast-policy.md
docs/design-system-harness.md
docs/design-system-review.md
docs/root-and-tokens-optimization.md
docs/sanitize-work-context-instructions.md
