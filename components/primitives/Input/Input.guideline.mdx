import { Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as InputStories from './Input.stories';
import { Input } from './Input';
import { Icon } from '../Icon';
import { DoDontExample } from '@sb-blocks/DoDontExample';
import { GuidelineToc } from '@sb-blocks/GuidelineToc';

{/*
  Meta of={InputStories} で Input.stories.tsx の Docs ページ本体としてこの mdx を使用する。
  Input.stories.tsx 側で tags: ['autodocs'] を付けないこと (二重生成になる)。
*/}
<Meta of={InputStories} name="Guideline" />

# Input

<GuidelineToc items={[
  { label: '概要', href: '#概要' },
  { label: 'Props', href: '#props' },
  { label: 'Do / Don\'t', href: '#do--dont' },
  { label: 'アクセシビリティ', href: '#アクセシビリティ' },
  { label: '関連', href: '#関連' },
]} />

## 概要

1 行のテキスト入力を受け付ける最小単位のフォームコンポーネント。**ラベル / ヘルプテキスト / エラーメッセージを一体管理** し、a11y 属性 (`aria-invalid` / `aria-describedby` / `aria-required`) を自動付与する。`error` と `errorMessage` は **discriminated union** で型レベルにセット必須化済で「エラー色だけで errorMessage 忘れ」が原理的に起きない。

`type` で `email` / `password` / `number` 等を切替えるとモバイルでキーボードレイアウトが最適化される。

---

## Props

下表は Input.tsx の TypeScript 型と JSDoc から自動生成。インタラクティブに props を試したい場合は **カタログの Playground story** を開き、Controls タブで操作する。

<ArgTypes of={InputStories.Playground} />

---

## Do / Don't

<DoDontExample
  label="ラベルは必ず指定する (placeholder で代用しない)"
  doExample={
    <Input label="メールアドレス" type="email" placeholder="example@email.com" />
  }
  doCaption="`label` を指定すると上にラベル + htmlFor 連携 + aria-* が自動で揃う。入力後もラベルが残るので何の欄か常に分かる。"
  dontExample={
    <Input placeholder="メールアドレス" type="email" />
  }
  dontCaption="placeholder だけだと、入力を始めるとラベルが消えて何の欄か分からなくなる。SR もラベルを読まないので a11y も損なう。"
/>

<DoDontExample
  label="必須は required prop で示す (自前 `*` で代用しない)"
  doExample={
    <Input label="ユーザー名" required helpText="3〜16 文字の英数字" />
  }
  doCaption="`required` を立てると `aria-required` 自動付与 + Label 内で `*` (aria-label='必須') が描画される。SR で「必須」が読まれる。"
  dontExample={
    <Input label="ユーザー名 *" helpText="3〜16 文字の英数字" />
  }
  dontCaption="ラベル文字列に手で `*` を埋め込むと SR で「*」を記号として読まれる (or 読まれない) ことがあり、必須が伝わらない。"
/>

<DoDontExample
  label="エラーは error + errorMessage をセットで指定する (型レベル必須)"
  doExample={
    <Input label="パスワード" type="password" error errorMessage="8 文字以上で入力してください" defaultValue="abc" />
  }
  doCaption="`error` を立てると `errorMessage` が型レベルで必須 (discriminated union)。aria-invalid + aria-describedby + role='alert' が自動連携し、SR がエラーを即時読み上げる。"
  dontExample={
    <Input label="パスワード" type="password" defaultValue="abc" className="!border-red-500 !bg-red-50" />
  }
  dontCaption="自前 className で枠だけ赤くしても aria 属性が立たず、SR ユーザーにエラーが伝わらない。色覚多様性のあるユーザーにも色の違いだけでは伝わらない。"
/>

<DoDontExample
  label="ヘルプとエラーは同時に出さない (本コンポーネントは自動)"
  doExample={
    <div className="flex flex-col gap-3">
      <Input label="ユーザー名" helpText="3〜16 文字の英数字" />
      <Input label="ユーザー名 (エラー時)" error errorMessage="3 文字以上で入力してください" defaultValue="ab" helpText="3〜16 文字の英数字" />
    </div>
  }
  doCaption="error 時は helpText が自動で非表示になり errorMessage に置き換わる (FormMessage 内部実装)。視覚的にも SR でも単一メッセージで明確。"
  dontExample={
    <div className="flex flex-col gap-1">
      <input className="h-12 rounded-sm border border-border-error-emphasis bg-surface-error-muted px-3" />
      <p className="text-xs text-onSurface-muted">3〜16 文字の英数字</p>
      <p className="text-xs text-onSurface-error">3 文字以上で入力してください</p>
    </div>
  }
  dontCaption="自前で input + help + error を組み立てると同時表示・冗長読み上げ・aria 関連付け漏れが起きやすい。Input コンポーネントに任せる。"
/>

<DoDontExample
  label="エラーは onBlur or 送信時に検証 (キーストロークごとは UX 悪化)"
  doExample={
    <Input label="メールアドレス" type="email" placeholder="example@email.com" helpText="フォーカスを外したタイミングで検証されます" />
  }
  doCaption="ユーザーがフィールドを離れる (onBlur) か送信ボタンを押した瞬間にバリデーション。入力途中に叱らないことで安心して入力できる。"
  dontExample={
    <Input label="メールアドレス" type="email" defaultValue="a" error errorMessage="正しいメールアドレスを入力してください" />
  }
  dontCaption="1 文字入力した直後にエラー表示するとユーザーが「途中なのに怒られている」感覚になり離脱率が上がる。"
/>

### 別コンポーネントの方が適切な場面

| やろうとすること | 使うべき別コンポーネント | 理由 |
|---|---|---|
| 複数行のテキスト (コメント・説明文) | `<Textarea>` | 改行できる入力は別コンポーネント |
| 数値スピナー (± ボタン / step 操作) | `<NumberInput>` | 数値の増減 UI は専用パターン |
| 候補表示 / 履歴付き検索 | `<SearchBar>` | 検索バーは UI 慣習が独立 |
| 選択肢から 1 つ選ぶ | `<Select>` / `<Radio>` / `<SegmentedControl>` | 「入力」ではなく「選択」 |
| 複数選択 | `<Checkbox>` / `<CheckboxGroup>` | フラグの組合せは別 UI |
| ON/OFF の真偽値 | `<Switch>` / `<Checkbox>` | 2 値は専用コントロール |
| 日付範囲 / カレンダー UI | (将来) `<DatePicker>` | `type="date"` はネイティブ UI のみ |

---

## 使用例（旧 story から移設・要精査）

> 旧 EdgeCases の usage 合成例。VR (Overview/EdgeCases) からは外し、ドキュメントとして保持。要不要は後で個別判断。

**ログインフォーム統合 — 複数 Input + error の組合せ**

<div className="w-80 flex flex-col gap-4">
  <Input label="メールアドレス" type="email" required fullWidth placeholder="example@email.com" />
  <Input label="パスワード" type="password" required fullWidth error errorMessage="パスワードが正しくありません" />
</div>

**Layout token 適用（`px-container` / `max-w-container-narrow` / `space-y-section-sm`）**

<form className="w-full px-container py-container max-w-container-narrow mx-auto bg-surface border border-border-subtle rounded-md">
  <div className="space-y-section-sm">
    <h3 className="text-heading-sm text-onSurface m-0">アカウント情報</h3>
    <div className="flex flex-col gap-4">
      <Input label="氏名" required fullWidth placeholder="山田 太郎" />
      <Input label="メールアドレス" type="email" required fullWidth placeholder="example@email.com" />
      <Input label="電話番号" type="tel" fullWidth placeholder="090-1234-5678" helpText="ハイフンありで入力" />
    </div>
  </div>
</form>

---

## アクセシビリティ

- **`<label htmlFor>` 自動連携**: `label` prop 指定で自動。クリックでフォーカス、SR がラベルを読む
- **`aria-required`**: `required` 時に自動付与
- **`aria-invalid`**: `error: true` 時に自動付与
- **`aria-describedby`**: `errorMessage` または `helpText` の id と自動関連付け
- **`role="alert"`** (FormMessage 内部): エラーメッセージ表示時に SR が即時通知
- **キーボード**: `Tab` でフォーカス、`Enter` でフォーム送信
- **タッチターゲット**: `size="md"` 以上で WCAG 2.5.5 (44x44px) を満たす。`small` は密集 UI 限定
- **placeholder 色**: `text-onSurface-muted` (#737373) で AA 4.69:1 を満たす
- **エラー色のみで伝えない**: `errorMessage` (型レベル必須) で言葉でも伝える

---

## 関連

### 内部で利用するコンポーネント

- `<Label>` — ラベル描画 + required `*` マーク + disabled 連動
- `<FormMessage>` (内部 `_internal/`) — helpText / errorMessage の表示と aria 関連付けを統括

### Input と組合せて使う代表的なコンポーネント

- `<Button>` — フォーム送信ボタン
- `<Checkbox>` / `<Radio>` — 同じフォーム内の選択コントロール
- `<Select>` — 選択肢のドロップダウン
- `<Textarea>` — 複数行入力 (Input の自然な拡張)
